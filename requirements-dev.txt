# Test / CI dependencies (laptop + GitHub Actions, not the Pi).
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-cov>=5.0

# Lint tooling, so `ruff check .` and `shellcheck deploy/*.sh` give the same
# answer locally as the Lint workflow does. Rules live in pyproject.toml under
# [tool.ruff]; they are error-level only, not style (see the note there).
# CI installs shellcheck from apt; shellcheck-py is the portable equivalent.
ruff>=0.6
shellcheck-py>=0.10

# The v1b scorer leg (GBT bundle, ablations, tail-mode radar bundle) is an
# optional runtime dependency (see requirements.txt) but its tests should run
# in CI rather than skip: the live bundle on the box is a GBT (2026-09-02).
scikit-learn>=1.3
joblib>=1.3
